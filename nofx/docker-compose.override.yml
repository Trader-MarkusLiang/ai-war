version: '3.8'

services:
  nofx:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    environment:
      - GOGC=50  # 更激进的GC，减少内存占用

  nofx-frontend:
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M
