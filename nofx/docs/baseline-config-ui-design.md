# Baseline 配置界面设计

## 1. 设计目标

### 1.1 核心需求
- ✅ 可视化 AI 策略参数 vs Baseline 指标参数
- ✅ 交互式配置 Baseline 参数
- ✅ 参数对齐检查和提示
- ✅ 实时预览参数效果
- ✅ 保存和复用配置

### 1.2 用户场景
1. **配置对齐检查** - 用户想确认 Baseline 和 AI 使用相同的基础参数
2. **参数调优** - 用户想调整 Baseline 指标参数以获得更好的对比效果
3. **策略对比** - 用户想了解 AI 决策逻辑 vs 传统指标逻辑的差异

## 2. 界面布局

### 2.1 位置
**策略工作室（Strategy Studio）** → 新增标签页 "Baseline 配置"

```
┌─────────────────────────────────────────────────────┐
│  策略工作室                                          │
│  ┌──────┬──────┬──────┬────────────┐               │
│  │ 基础 │ 指标 │ 风控 │ Baseline ✨│               │
│  └──────┴──────┴──────┴────────────┘               │
└─────────────────────────────────────────────────────┘
```

### 2.2 布局结构

```
┌─────────────────────────────────────────────────────────────┐
│  Baseline 策略配置                                          │
│                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐         │
│  │   AI 策略参数       │  │  Baseline 指标参数  │         │
│  │   (只读，参考)      │  │  (可编辑)           │         │
│  ├─────────────────────┤  ├─────────────────────┤         │
│  │ 杠杆: 5x           │  │ RSI 周期: 14        │         │
│  │ 止损: 2%           │  │ MACD 快线: 12       │         │
│  │ 止盈: 5%           │  │ MACD 慢线: 26       │         │
│  │ 仓位: 20%          │  │ EMA 周期: 20        │         │
│  └─────────────────────┘  │ StochRSI: 14        │         │
│                           │ ATR 周期: 14        │         │
│  ┌─────────────────────┐  └─────────────────────┘         │
│  │   共享参数          │                                   │
│  │   (自动同步)        │  ┌─────────────────────┐         │
│  ├─────────────────────┤  │  信号阈值配置       │         │
│  │ ✅ 杠杆配置        │  ├─────────────────────┤         │
│  │ ✅ 手续费          │  │ RSI 超卖: 30        │         │
│  │ ✅ 滑点           │  │ RSI 超买: 70        │         │
│  │ ✅ 交易对         │  │ StochRSI 超卖: 20   │         │
│  └─────────────────────┘  │ StochRSI 超买: 80   │         │
│                           │ 信号共振数: 2       │         │
│                           └─────────────────────┘         │
│                                                             │
│  ┌─────────────────────────────────────────────┐          │
│  │  参数对齐检查                                │          │
│  │  ✅ 杠杆配置已同步                           │          │
│  │  ✅ 风险参数已同步                           │          │
│  │  ⚠️  建议: Baseline 使用更保守的止损        │          │
│  └─────────────────────────────────────────────┘          │
│                                                             │
│  [保存配置]  [重置为默认]  [预览信号]                      │
└─────────────────────────────────────────────────────────────┘
```

## 3. 功能模块

### 3.1 参数对比面板

**左侧：AI 策略参数（只读）**
- 显示当前策略的关键参数
- 用于参考和对比
- 灰色背景，不可编辑

**右侧：Baseline 参数（可编辑）**
- 技术指标周期参数
- 信号阈值配置
- 风险管理参数

### 3.2 共享参数面板

显示自动同步的参数：
- ✅ 杠杆配置（BTC/ETH、山寨币）
- ✅ 手续费率
- ✅ 滑点
- ✅ 交易对列表
- ✅ 时间周期

### 3.3 参数对齐检查

实时检查并提示：
- ✅ 参数已对齐
- ⚠️ 参数差异警告
- 💡 优化建议

### 3.4 预览功能

点击"预览信号"按钮：
- 显示最近 N 根 K 线的信号
- 标注买入/卖出信号点
- 对比 AI 决策 vs Baseline 信号

## 4. 数据结构

### 4.1 后端扩展

在 `StrategyConfig` 中添加 Baseline 配置：

```go
type StrategyConfig struct {
    // ... 现有字段

    // Baseline 配置
    BaselineConfig *BaselineConfig `json:"baseline_config,omitempty"`
}

type BaselineConfig struct {
    // 指标参数
    IndicatorConfig IndicatorConfig `json:"indicator_config"`

    // 信号阈值
    SignalThresholds SignalThresholds `json:"signal_thresholds"`

    // 风险管理
    RiskManagement BaselineRiskManagement `json:"risk_management"`
}

type SignalThresholds struct {
    RSIOversold       float64 `json:"rsi_oversold"`        // 默认 30
    RSIOverbought     float64 `json:"rsi_overbought"`      // 默认 70
    StochOversold     float64 `json:"stoch_oversold"`      // 默认 20
    StochOverbought   float64 `json:"stoch_overbought"`    // 默认 80
    MinSignalCount    int     `json:"min_signal_count"`    // 默认 2
}

type BaselineRiskManagement struct {
    TakeProfitATR float64 `json:"take_profit_atr"` // 默认 2.0
    StopLossATR   float64 `json:"stop_loss_atr"`   // 默认 1.0
    PositionSize  float64 `json:"position_size"`   // 默认 0.2 (20%)
}
```

### 4.2 前端组件

新建组件：`BaselineConfigPanel.tsx`

```typescript
interface BaselineConfigPanelProps {
  strategyId: string
  strategyConfig: StrategyConfig
  onSave: (config: BaselineConfig) => void
}
```

## 5. 交互流程

### 5.1 配置流程

```
1. 用户进入策略工作室
   ↓
2. 点击 "Baseline 配置" 标签
   ↓
3. 查看 AI 参数 vs Baseline 参数对比
   ↓
4. 调整 Baseline 参数（滑块/输入框）
   ↓
5. 实时显示参数对齐检查结果
   ↓
6. 点击"预览信号"查看效果（可选）
   ↓
7. 点击"保存配置"
   ↓
8. 配置保存到策略中
```

### 5.2 回测使用流程

```
1. 启动回测时勾选"启用基线对比"
   ↓
2. 系统读取策略的 BaselineConfig
   ↓
3. 使用配置的参数运行 Baseline
   ↓
4. 结果页面显示对比曲线
```

## 6. 实现优先级

### Phase 1: 基础配置界面 ⭐⭐⭐
- [ ] 创建 BaselineConfigPanel 组件
- [ ] 添加到策略工作室标签页
- [ ] 实现参数编辑表单
- [ ] 保存配置到策略

### Phase 2: 参数对比和检查 ⭐⭐
- [ ] AI 参数展示面板
- [ ] 共享参数自动同步
- [ ] 参数对齐检查逻辑
- [ ] 优化建议提示

### Phase 3: 预览功能 ⭐
- [ ] 信号预览图表
- [ ] 历史数据回放
- [ ] AI vs Baseline 信号对比

## 7. UI 设计细节

### 7.1 颜色方案
- AI 参数面板：蓝色主题 (#3B82F6)
- Baseline 参数面板：橙色主题 (#FF6B35)
- 共享参数：绿色主题 (#10B981)
- 警告提示：黄色 (#F59E0B)

### 7.2 交互元素
- **滑块** - 用于周期参数（14-50）
- **数字输入框** - 用于精确值
- **开关** - 用于启用/禁用功能
- **提示气泡** - 参数说明

### 7.3 响应式设计
- 桌面：左右分栏布局
- 平板：上下堆叠布局
- 手机：单列布局，可折叠面板

## 8. 技术实现要点

### 8.1 状态管理
```typescript
const [baselineConfig, setBaselineConfig] = useState<BaselineConfig>()
const [alignmentCheck, setAlignmentCheck] = useState<AlignmentResult>()
```

### 8.2 实时验证
```typescript
useEffect(() => {
  // 参数变化时，实时检查对齐情况
  const result = checkParameterAlignment(strategyConfig, baselineConfig)
  setAlignmentCheck(result)
}, [baselineConfig, strategyConfig])
```

### 8.3 API 接口
```typescript
// 保存 Baseline 配置
PUT /api/strategies/{id}/baseline-config

// 获取 Baseline 配置
GET /api/strategies/{id}/baseline-config

// 预览信号
POST /api/strategies/{id}/baseline-preview
```

---

**文档版本**: v1.0
**创建日期**: 2026-01-02
**作者**: NOFX Team
